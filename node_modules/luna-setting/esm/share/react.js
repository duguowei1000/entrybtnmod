import { jsx as _jsx } from "react/jsx-runtime";
import { classPrefix, getPlatform } from './util';
import className from 'licia/className';
export const Component = (props) => {
    const c = classPrefix(props.compName);
    return (_jsx("div", { className: className(`luna-${props.compName}`, c(`platform-${getPlatform()}`), c(`theme-${props.theme || 'light'}`), props.className), children: props.children }));
};
