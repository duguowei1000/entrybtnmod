import Emitter from 'licia/Emitter';
declare class Observer extends Emitter {
    private observer;
    constructor();
    observe(node: Node): void;
    disconnect(): void;
    private handleMutation;
}
declare const _default: Observer;
export default _default;
